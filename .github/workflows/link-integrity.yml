name: Link Integrity

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Link integrity check disabled — zencloud.com.au blocks CI runner IPs via bot protection"
